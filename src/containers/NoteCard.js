import React from 'react';
import Card from '@material-ui/core/Card';

export const NoteCard = (props) => {
    console.log('props in notecard: ', props);
    // const { classes } = this.props;
  return (
    <div>
    </div>

  )
}
